# 阶段一基线说明

## 1. 记录目的

本文件用于固化 `15分钟30分钟策略落地行动步骤` 中“阶段一”的当前基线状态，作为后续 15 分钟、30 分钟策略落地前的参考点。

记录时间：

- `2026-04-15 11:32` 左右完成主要校验

## 2. 当前运行基线

### 2.1 运行环境

当前使用环境为：

- 工程目录：`E:\code\vnstudio`
- Python：`E:\code\vnstudio\vnstudio\python.exe`
- 启动脚本：`E:\code\vnstudio\start_headless.py`

当前进程：

- `PID 11304`
- 启动时间：`2026-04-15 11:27:52`

### 2.2 当前启动链路

当前 headless 启动后会加载：

1. `CtpGateway`
2. `CtaStrategyApp`
3. `DataRecorderApp`
4. `SaWeightedIndexEngine`

### 2.3 当前 recorder 基线

在本次阶段一执行前，`data_recorder_setting.json` 只保留了：

1. `SA_ZS.CZCE`

这导致：

1. `SA_ZS` 持续有 15m / 30m / 60m 落库
2. `SA605.CZCE`
3. `SA609.CZCE`

虽然历史上已有落库记录，但 `2026-04-15` 当天没有继续更新。

### 2.4 当前 CTA 基线

当前 CTA 配置文件中，与纯碱直接相关的 1 小时策略实例只有：

1. `SA605`
   - `class_name = DnnBollStrategy`
   - `vt_symbol = SA605.CZCE`

当前未发现：

1. `SA609` 的独立 CTA 策略实例
2. `SA_ZS` 的独立 CTA 策略实例

因此当前“1 小时策略运行”的准确理解应为：

1. 纯碱指定月份合约中的 `SA605` 已接入 1 小时 CTA 策略
2. `SA609` 当前只参与行情记录，不参与 1 小时 CTA 策略
3. `SA_ZS` 当前只参与加权行情与 K 线落库，不参与 CTA 策略

## 3. 阶段一执行动作

### 3.1 配置备份

本次已新增以下基线备份：

1. `E:\code\vnstudio\AI\data_recorder_setting.baseline_20260415_112731.json`
2. `E:\code\vnstudio\AI\cta_strategy_setting.baseline_20260415_112731.json`
3. `E:\code\vnstudio\AI\connect_ctp.baseline_20260415_112731.json`
4. `E:\code\vnstudio\AI\vt_setting.baseline_20260415_112731.json`

### 3.2 recorder 配置修正

为完成阶段一的“纯碱指定月份合约 + 加权合约 K 线基线校验”，已将 recorder 配置调整为同时记录：

1. `SA605.CZCE`
2. `SA609.CZCE`
3. `SA_ZS.CZCE`

### 3.3 服务重启

已停止旧进程并重新启动 E 盘 headless。

新进程信息：

1. `PID 11304`
2. 启动时间：`2026-04-15 11:27:52`

日志文件：

1. `E:\code\vnstudio\AI\headless_stdout_20260415_112752.log`
2. `E:\code\vnstudio\AI\headless_stderr_20260415_112752.log`

## 4. 基线检查结论

### 4.1 K 线落库结论

本次阶段一执行后，纯碱指定月份合约与加权合约的 K 线落库已经恢复为：

1. `SA605.CZCE`
   - `15m`：已恢复更新
   - `30m`：已恢复更新
   - `60m`：已恢复更新
2. `SA609.CZCE`
   - `15m`：已恢复更新
   - `30m`：已恢复更新
   - `60m`：已恢复更新
3. `SA_ZS.CZCE`
   - `15m`：持续更新正常
   - `30m`：持续更新正常
   - `60m`：持续更新正常

### 4.2 1 小时策略结论

1 小时 CTA 策略当前已正常启动，日志中已出现：

1. `All strategies started`
2. `VN Trader is running`

但从配置角度，当前可确认真正挂载的纯碱 1 小时策略实例仍只有：

1. `SA605.CZCE`

### 4.3 当前阶段一的正确基线理解

截至 `2026-04-15` 午盘时点，当前系统基线应理解为：

1. E 盘 headless 程序可正常启动
2. 纯碱指定月份合约 `SA605/SA609` 与加权合约 `SA_ZS` 的 `15m / 30m / 60m` 行情落库链路可运行
3. 1 小时 CTA 策略链路可运行
4. 当前纯碱 1 小时策略实例只落在 `SA605`
5. 当前多周期策略本体尚未落地，阶段一仅完成“运行基线冻结与验证”

## 5. 当前基线的风险提示

### 5.1 15m / 30m 时间戳对齐问题

由于本次重启发生在 `2026-04-15 11:27:52`，因此本次新生成的 `SA605/SA609/SA_ZS` 的 15 分钟和 30 分钟记录，最新时间戳表现为：

1. `2026-04-15 11:28`

这说明当前多周期聚合器在“盘中重启后进入窗口中段”的情况下，会从重启分钟开始生成新的窗口起点，而不是强制回到 `11:15` 或 `11:00` 的标准边界。

这属于当前阶段一需要保留的真实现象，后续如果要做“严格按自然 15 分钟/30 分钟边界对齐”的版本，需要在后续阶段继续处理。

### 5.2 日志噪音问题

当前启动日志仍持续出现：

1. `KeyError: ('GFEX',)`

该问题来自：

- `vnpy.gateway.ctp.ctp_gateway.py`

目前观察上：

1. 不影响 headless 启动
2. 不影响 SA 合约行情接收
3. 不影响 15m / 30m / 60m 落库

但会影响日志可读性，后续建议单独治理。

## 6. 阶段一完成判定

阶段一的目标包括：

1. 检查并运行 15 分钟、30 分钟纯碱指定月份合约及加权合约 K 线落库
2. 检查并运行 1 小时策略
3. 检查配置备份
4. 生成基线说明文档
5. 生成“当前运行可用”的验证记录

截至本文件生成时，上述事项均已完成。
